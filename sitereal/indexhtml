  if (currentBiome === "neon") {
  if (Math.random() > 0.5) {
    // PEDRA
    width = 180;
    height = 190;
    image = "./fotos/pedrafloresta.png";
    hitboxScaleX = 0.52;
    hitboxScaleY = 0.58;
    bottomOffset = -15; // desce um pouco pra encaixar no chão
  } else {
    // ÁRVORE
    width = 180;
    height = 190;
    image = "./fotos/arvore2.0.png";
    hitboxScaleX = 0.62;
    hitboxScaleY = 0.62;
    bottomOffset = -28; // encaixa melhor no chão
  }
}